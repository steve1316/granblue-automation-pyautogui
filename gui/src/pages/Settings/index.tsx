import { Box, Fade } from "@mui/material"
import "./index.scss"

const Settings = () => {
    return (
        <Fade in={true}>
            <Box className="container"></Box>
        </Fade>
    )
}

export default Settings
